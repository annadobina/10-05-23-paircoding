let obj = {
    width: 2,
    length: 5,
    height: 1
};

function volumeOfContainer(obj.[width], obj.[length], obj.[height]) {
    return obj.width*obj.length*obj.height;
}
console.log(volumeOfContainer())